// algorithms/trainStatus.js
const getStatus = (trainNumber) => {
    // Yahan aap apna logic ya Railway API integration likhenge
    return `Train ${trainNumber} is running on time!`;
};

module.exports = { getStatus };